# GuardianLink Backend Configuration
PORT=3001
HOST=0.0.0.0

# JWT Secret (change in production!)
JWT_SECRET=guardianlink-dev-secret-change-in-production
JWT_EXPIRES_IN=7d

# SQLite database path (defaults to ./data/guardianlink.db)
GUARDIANLINK_DB_PATH=./data/guardianlink.db